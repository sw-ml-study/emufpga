Made .spm encoding-aware and added the f32 profile. Encoding::bytes_for is now the single answer to a group's byte length and every call site asks it; packed_len went back to being ternary's implementation detail. Added spm-codec-dense (four little-endian bytes per weight) and SpmWriter::write_raw_group, the encoding-neutral path that checks payload length against what the stream's encoding implies. Extension not change: version_major unmoved, ternary golden fixture byte-identical with its test passing unmodified. The test that matters writes a ternary stream and an f32 stream into one file and reads both back, which a per-file sizing rule could not do. Dense f32 asserted lossless bitwise including NaN by to_bits. Also, prompted by the GitHub size limits: broadened .gitignore from a five-extension allowlist to default-deny for binaries and archives, added scripts/check-size as a gate failing on any file over 1 MiB tracked or untracked, and recorded in CLAUDE.md that a large file entering the repo is Mike's explicit call. Verified no .jsonl was ever committed in any commit. 130 tests, 0 failed.