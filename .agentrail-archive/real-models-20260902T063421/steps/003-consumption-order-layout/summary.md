Laid TRM out in consumption order and proved a forward sweep never seeks backward. Fixed a real defect in step 2: the extractor's alphabetical sort is the reverse of TRM's per-layer execution order, so the file shipped in 85e5b2b would have needed to seek backward. layouts/trm-maze-30x30.order specifies the order in two sections, rotating first because rewind returns to stream 0. Rotating is 8 streams and 99.87% of weights; resident is 7 streams and 0.13%. Tested against the real tracked order file with no download. A second test pins the alphabetical mistake so it cannot return. One expectation was mine again -- 29 rewinds instead of 14, having forgotten one L_level call sweeps both layers -- and the count is now derived rather than written down.