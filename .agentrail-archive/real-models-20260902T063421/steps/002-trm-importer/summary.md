Imported the real TRM checkpoint into .spm byte for byte: 15 tensors, 6,824,450 parameters, 27,324,980 bytes, zero mismatches on round trip, byte accounting closing exactly. Split at the pickle boundary -- stdlib Python extractor (torch not required), Rust importer that is pure framing since the extractor's blobs are already the f32 wire format. Names go in a sidecar because the FPGA streams bytes in directory order and never needs them. Tests are hermetic on synthetic checkpoints; the real file is a manual verification in docs/results.md. One test failure was mine rather than the code's: I asserted the wrong ramp value for a short final group, and the fixture now derives expectations from a named function so they cannot drift from the data.