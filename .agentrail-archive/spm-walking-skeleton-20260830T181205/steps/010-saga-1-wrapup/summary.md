Closed saga 1. Wrote docs/architecture.md as the map between the other documents; led README with the results and their caveats. Audited docs/plan.md against the tree and found two real drifts: components/engine/ and components/report/ were empty scaffolding from step 001 promising crates the scope correction eliminated (removed -- the engine IS fabric-model and reporting is per-component), and the plan still listed gowin-budget/gowin-timing from the withdrawn fit report. Defined saga 2 from what saga 1 measured, ordered by evidence: overlapped fetch first (it is the single caveat on every number in results.md and the cheapest to remove), then Q4 and bitplane layouts (every conclusion so far is really about BitNet-style ternary), then TRM. Explicitly excluded hardware, since bulk bandwidth and fmax remain Unknown. Final: 122 tests, sw-checklist 141 passed / 0 failed / 1 warning, no #[allow] anywhere -- zero held from the first crate rather than paid down.