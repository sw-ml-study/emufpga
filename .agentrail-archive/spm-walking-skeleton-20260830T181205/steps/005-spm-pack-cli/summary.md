Added components/cli/: spm-quantize (dependency-free dense f32 to ternary .spm), emufpga-cli (clap dispatch), and the emufpga binary. Switched from hand-rolled parsing to clap on Mike's call, which turned out to be required anyway: sw-checklist has a Clap Dependency check. Read sw-checklist's CLI rules from its own output rather than guessing, and all now pass -- --help longer than -h, an AI CODING AGENT INSTRUCTIONS section carrying the two invariants an agent most needs to not break, -V identical to --version, and Copyright/License/Repository/Build Host/Build Commit/Build Time from a build.rs that falls back to 'unknown' rather than failing the build. Quantization is per-group absmean (BitNet b1.58): scale = mean(|w|), t = clamp(round(w/scale), -1, 1), ties away from zero, and an all-zero group written as scale 1.0 with Zero weights so a zero scale never reaches the wire format. Every rule pinned by a hand-computed test rather than left implicit. Shape is inferred from the input file rather than passed as flags. The test that matters packs a matrix, streams it through spm-gemv-ref, and compares against spm-numeric -- the packer is only correct if the engine reproduces the reference from its output. A second test asserts the CLI writes byte-identical output to the library so a second packing path cannot drift into existence. 19 tests here, 98 across four components. One sw-checklist warning remains: Binary Freshness wants sw-install, which the checkpoint process forbids running unasked.