Built components/tensor/: six crates giving the CPU golden reference engine. The datapath lives in its own file so the RTL author has one place to read: NONZERO_BIT is the accumulator enable, NEGATIVE_BIT the add/subtract select, no multiplier between them, and group scales fold into the activation rather than each weight. Found that the step prompt (and the Ternary2F32I32 profile name) had settled the accumulator width question without it being worked out: because scale groups run along a column-major stream, a group's scale can vary with both row and column, so keeping the inner loop multiplier-free means either f32 accumulation with a pre-scaled activation (exact) or i32 with fixed-point pre-scaling (cheaper, lossy). The reference takes the exact one since an oracle carrying its own quantization error cannot adjudicate anyone else's; saga 2 decides with the fabric to measure. No format change needed. The economic argument now runs end to end: across batch 1..32 a real scan asserts Ps equals the batch size while parameter_bytes_read is unchanged. Tolerance 2e-3 derived from f32 summation error, set once and not tuned. Two findings: a zero-weight 1x1 case exposed a test that treated cosine's undefined None as divergence (engine right, assertion wrong), and cosine returned 1.0000000000000002 on an exact match so the function now clamps to its defined range. 79 tests, 0 warnings, no #[allow].