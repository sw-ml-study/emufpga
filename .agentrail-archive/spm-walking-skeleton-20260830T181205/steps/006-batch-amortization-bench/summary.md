Built emufpga bench and ran the make-or-break measurement. Headline is a negative result with a usable number: the crossover does not exist in range -- eta is 0.005 at batch 1 and falls, so the CPU reference is compute-bound everywhere and the crossing lies below the smallest batch swept. Rather than report 'crossover at batch 1', Crossover is a three-armed enum (AlreadyBelow/At/NotReached) and the report prints NOT MEASURED. The useful form is the ratio: the engine is ~196x too slow to saturate even a page-cached file read, which is the target step 008's fit model must close, and a lower bound since a warm read overstates any real store. Second finding: batching raises useful work per byte AND lowers byte consumption rate simultaneously (128x batch -> 44x aggregate throughput, 2.9x lower consumption), so the reference is firmly in the compute-bound regime; no knee, per-lane efficiency declines monotonically. Found and fixed a real 5x bug: spm-accum stored accumulators lane-major, striding 4 KiB per lane, and throughput collapsed at batch 64 when the working set passed L1; row-major storage removed the cliff and matches the hardware where a row's lanes are adjacent registers. Timer overhead measured (40 ns/pair) and reported so the storage/compute split can be checked as resolvable. Everything the numbers do not support is printed with them. 103 tests, 0 failed.