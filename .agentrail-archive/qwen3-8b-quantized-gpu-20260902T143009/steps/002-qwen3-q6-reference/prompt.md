Acquire the official Qwen/Qwen3-8B-GGUF Q6_K artifact under /disk1/tmp without placing weights in Git. Pin and record the Hugging Face repository revision, filename, byte size, and SHA-256; verify GGUF metadata using trusted tooling. Confirm available llama.cpp and Ollama import paths, choosing the least transformational path that preserves the official GGUF bytes. Run the existing fixed Qwen3 benchmark at the same 8192-token context, seed, prompt, temperature, and generation limit, recording full-GPU placement, whole-device peak VRAM, timing, throughput, and output identity. Compare Q6_K with the Q4_K_M baseline and state whether Q8_0 is likely to add useful fidelity evidence. Add only scripts or concise docs needed for reproducibility; keep models and raw results in /disk1/tmp. Run the full project pre-commit gate, commit source and agentrail metadata, complete and push. At completion summarize completed work, next steps, blockers, and choices/tradeoffs.