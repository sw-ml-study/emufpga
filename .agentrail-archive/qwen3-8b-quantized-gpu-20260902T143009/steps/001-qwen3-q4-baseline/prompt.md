Characterize the locally installed Ollama qwen3:8b Q4_K_M model on the RTX 5060 Ti as the reproducible baseline for later Rust and quantization comparisons. Confirm exact model metadata and content identity without copying weights into Git. Add a small deterministic, reusable benchmark harness using repository-native shell/Rust conventions: fixed prompts, fixed generation parameters, 8192-token context cap, warmup plus measured runs, and captures for wall time, token throughput when exposed, GPU placement, and peak/observed VRAM. Ensure outputs and large traces go under /disk1/tmp, while Git receives only scripts, concise metadata/results, and instructions. Verify the harness against the running local Ollama service, document limitations of token-level determinism and server caching, run the full project pre-commit gate, commit all source and agentrail metadata, complete the step, and push. At completion summarize completed work, next steps, blockers, and choices/tradeoffs.