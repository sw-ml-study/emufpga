Build the next correctness rung from the validated Qwen3 Q6_K GGUF. First implement or integrate exact Q6_K and F32 tensor decoding only after spm-gguf validation, with hand-checkable block fixtures and selected-tensor hashes/statistics. Select a maintained Rust execution backend only if it can consume the validated file/ranges and support this RTX 5060 Ti; keep the trust-boundary parser in front. Establish an independent logits oracle: prefer llama.cpp built at a pinned revision with a logits-capable deterministic command, or a documented Candle CPU comparison if llama.cpp cannot be obtained. Implement the smallest meaningful Qwen3 forward slice before full generation—embedding plus one transformer block, then final logits if feasible—using fixed tokens and explicit tolerances. Do not claim token parity from Ollama text alone. Reduce the new GGUF complexity warnings where the execution integration provides natural abstractions. Keep all large artifacts under /disk1/tmp. Run the full gate, commit source and agentrail metadata, complete and push. At completion summarize completed work, next steps, blockers, and choices/tradeoffs.