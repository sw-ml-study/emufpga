Replace scripts/extract-checkpoint with a Rust-native checkpoint ingestion implementation while preserving the Python extractor as an independent oracle during migration. Support the exact PyTorch .pt pickle/ZIP subset and safetensors dtypes currently used by TRM, HRM, and SmolLM; emit the same manifest and blobs in .spm stream order, including f32 widening, bf16 round-to-nearest-even, and 2-D transpose behavior. Add hermetic parity tests and run parity against the restored real checkpoints under /disk1/tmp/spm-weights without adding large artifacts to git. Add an sw-MLPL executable specification for the layout transformation if it fits the existing MLPL surface. Update scripts and documentation so Rust is the normal extraction path and Python remains a conformance oracle. Run the full project gate, commit source plus agentrail metadata, complete the step, and push.