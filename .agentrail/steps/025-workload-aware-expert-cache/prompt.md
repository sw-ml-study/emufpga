Using the pinned oversized Gemma-4 Q5_K_M model, build a realistic but focused same-language coding-agent workload (begin with Rust; retain deterministic executable acceptance tests and include sufficiently varied repository-style tasks). Trace expert routing per layer and token across repeated single and concurrent runs; establish frequency, reuse-distance, overlap, phase/prompt-vs-generation, and stability rankings with confidence intervals. Define honest baselines: ordinary resident/page-cache behavior, unconditional reclaimed experts, and no-policy cold demand paging. Implement and measure (1) a fixed preloaded cache of highest-ranked layer-expert tensors under explicit RAM budgets and (2) an online demand-adaptive cache using recency/frequency and admission/eviction hysteresis, without changing logits or quantization. Prevent train/test leakage by deriving rankings on a calibration task split and evaluating on held-out tasks plus language/domain-shift controls (C and assembly if feasible). At concurrency 1/2/4/8, at least three alternating repetitions where feasible, report task pass rate, generated and accepted tokens/s, passing tasks/hour, latency distribution, physical and logical bytes, cache hit and byte-hit rates, expert reloads, RSS/PSS/VRAM, CPU/GPU utilization, and board/system energy where measurable. Compare static ranking, adaptive caching, resident, and unconditional reclamation; identify cache-size knees and whether gains persist for multiple coding agents. Treat speed as usability and model capacity/correctness as primary success. Check in raw summaries, methodology, caveats, conclusions, README and live Results graphics; run the full gate, commit main, push, and deploy Pages.