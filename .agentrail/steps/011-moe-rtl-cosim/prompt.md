Build the next honest validation layer for the purpose-built serial-MoE FPGA datapath: a hardware-shaped cycle model and, where local tools permit, small synthesizable RTL plus co-simulation for the input FIFO/backpressure, exact Q6_K 210-byte block decoder, configurable MAC lanes, and accumulator BRAM. Consume exact committed fixture or .spm bytes and compare decoded values, partial sums, final outputs, byte counts, and ordering against the Rust oracle across known-answer and deterministic randomized cases. Add truncation/corruption/backpressure failure tests. Export cycle events that the existing visualization can replay. Report separately measured host/co-sim results and projected FPGA clock/resource/bandwidth figures; do not claim physical timing or fit without synthesis/place-and-route. Keep large artifacts under /disk1/tmp, use scripts/serial.sh for Cargo, run scripts/check --all, commit directly to main, complete Agentrail with a concrete successor, and push.