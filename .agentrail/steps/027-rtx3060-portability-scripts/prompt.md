Port the GPU/model experiment scripts off the large12-specific assumptions (hardcoded CUDA sm_120a and /disk1 paths) so the same Gemma-4 too-large-to-fit workaround and sibling experiments run on this RTX 3060 12G (Ampere sm_86) box. Add scripts/cuda-arch.sh autodetect, SPM_SCRATCH/SPM_SRC path indirection, relax the host-specific device:inode model pin to size-only, and document the port + test plan in docs/rtx3060-port.md.