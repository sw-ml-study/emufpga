Move the validated bounded expert provider from the logits oracle into the pinned long-lived llama.cpp server path behind an explicit opt-in configuration. Correct telemetry so cache hits and source bytes are reported per expert operation as well as per worker. Compare ordinary LRU with a layer-aware or profile-guided policy that avoids evicting an entry merely because later layers separate token uses. First run deterministic complete-logit/provider controls. Then, once CUDA and nvidia-smi are stable, run the existing identical same-quant executable Rust coding workload at c1/c2/c4/c8 against conventional mmap/reclamation and the native provider, with repeated paired trials and controlled cache state. Report correct tasks, accepted/generated tokens per second only as usability context, aggregate useful work, physical/source bytes, expert wait, RSS/PSS, VRAM, hit/eviction/reload rates, and confidence. The success question is whether concurrent agents share expert loads and preserve quality/capacity, not whether one request beats llama.cpp. Update data/docs/live, gate, commit main, push, and deploy Pages.