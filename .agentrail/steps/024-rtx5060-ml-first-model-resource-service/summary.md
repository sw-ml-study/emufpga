Built the minimal RTX 5060 ML-first user-space resource service in Rust. Added strict JSON/SHA-256 content-addressed manifests with immutable model/tensor/expert streams, mutable KV/activation contracts, tier metadata, and bounded residency; non-JSON executable serialization and mismatched payloads are rejected. Added a Unix-domain socket batch protocol that collects identical content IDs and serves multiple clients from one forward-only 2 MiB double-buffer sweep. Verified two live clients received the same 169,688,672-byte Gemma digest. In 3 cold repetitions at c1/c2/c4/c8, shared c8 traversal used 169.69 MB and 2 MiB versus independent 1.36 GB and 8 MiB, with 961.8 versus 1,263.2 ms p50. Both policies caused the same 169.69 MB physical HDD read because Linux page cache already shares concurrent reads; this is a user-space coordination result, not an 8x disk saving or inference-quality result. Documented prospective OS interfaces and unmeasured GPU, NUMA, cgroup, energy, cancellation, and full-compute work. Updated and built live Results/Lab graphics. All Rust/data/visualization gates pass; unchanged inherited sw-checklist baseline is 9 failures/48 warnings.