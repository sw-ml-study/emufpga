Build a fair physical-stream comparison for reused SAS storage. Keep the pinned Gemma-4 Q5_K_M canonical weights on HDD, create a deterministic execution-order expert stream or trace-replay slice that never requires the full stream resident, and consume it through bounded asynchronous double buffers. Compare ordinary 4 KiB mmap demand faults against: one sequential HDD stream, independent shards on parallel SAS HDDs when the target host is available, a prepopulated SSD hot-expert cache with HDD backing, and a complete SSD stream control. Preserve identical expert bytes and activation math; validate results against the existing oracle. Measure per-device bytes, IOPS, read-size distribution, queue depth, sequential bandwidth, cache hits and writes, buffer residency, NUMA/core placement, expert reuse, task correctness, latency, aggregate completions, power, and endurance implications. Distinguish measured results from projected multi-drive scaling. Update documentation and live visualization, run the project gate, commit to main, push, and deploy Pages.