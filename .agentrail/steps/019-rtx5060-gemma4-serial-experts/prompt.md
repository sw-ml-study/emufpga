Implement the same-quant Gemma-4 Q5_K_M ordered selected-expert path needed to compare against the measured conventional 20-layer GPU offload baseline. First validate one real Gemma-4 expert layer against an independent GGUF oracle with bounded parameter residency and batch reuse at 1/2/4/8 activations. Then design the least invasive end-to-end integration that keeps attention, KV cache, routing, and shared tensors on GPU while executing or staging selected experts serially; instrument bytes, RSS/VRAM, latency, throughput, correctness, and clearly scoped energy. Preserve the 128+16 smoke contract for direct comparison before attempting the predeclared 4K+256 qualification. Update the claim scorecard and live Results page with measured data and caveats. No FPGA programming.