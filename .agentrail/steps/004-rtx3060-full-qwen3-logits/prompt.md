Generalize the validated five-token Qwen3 Rust path from block 0 across all 36 transformer layers. Dynamically resolve per-layer tensor names, maintain a distinct KV cache per layer, preserve the invariant that at most one decoded projection matrix is resident at once, then implement final RMS normalization and output projection. Compare final-position logits and top-10 tokens against a strict llama.cpp CPU oracle, capturing additional callback artifacts only where needed to localize drift. Add focused tests for layer/cache separation and final ranking, document explicit numerical tolerances and memory behavior, run ./scripts/check --all, commit source plus agentrail metadata, complete the step, rename feat/full-qwen3-logits to pr/full-qwen3-logits, and push.