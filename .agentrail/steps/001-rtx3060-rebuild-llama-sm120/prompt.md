Rebuild and pin the existing Linux llama.cpp checkout for the RTX 5060 Ti (Blackwell, compute capability 12.0). First inspect the installed CUDA toolchain and upstream build configuration; do not overwrite the known older build. Build in a separate directory under /disk1/tmp or another non-repository artifact location. Run the Qwen3 8B Q6_K logits oracle with full GPU offload, verify that CUDA kernels execute, and compare token IDs/top-ten logits against the checked-in deterministic CPU golden. Repeat the CUDA run to measure determinism and define an explicit tolerance if raw bits differ. Record llama.cpp revision, compiler/CUDA versions, build flags, GPU memory, output, choices, and blockers. Update the wrapper to select the validated build without weakening its revision pin. Run the complete project pre-commit gate, commit source and agentrail state, complete the step, and push the feature branch.
