Replace the weak Gemma coding text-containment smoke with a safe executable same-quant qualification. Define a small versioned Rust coding corpus with fixed function signatures and hidden deterministic tests. Query the identical pinned Gemma-4 Q5_K_M model through (A) resident lazy CPU experts without reclamation and (B) the bounded MADV_RANDOM plus MADV_DONTNEED selected-expert path at 1/2/4/8 independent requests using the same prompts and decoding policy. Extract code defensively, compile dependency-free sources, and execute only inside Docker with network disabled, read-only root filesystem, explicit CPU/memory/PID/time limits, and disposable storage; never use pickle or execute generated code directly on the host. Report compile rate, test-pass rate, strict-format compliance, per-task disagreements, VRAM/RSS/PSS, expert assignments/unions/logical bytes, and usability timing without treating speed as a success gate. Repeat enough times to distinguish batching variation. Update checked-in data, claim scorecard, Gemma analysis, README, and live Results graphics with honest caveats. Also add a follow-up plan for cold-cache/direct-I/O measurement because warm mmap process read_bytes cannot measure SSD demand. Run the full gate, document inherited sw-checklist debt without worsening it, commit to main, push, and deploy Pages.