Built components/fabric/: the conceptual fabric model and emufpga sim, per Mike's scope correction. Abstract knobs only -- weight lanes, batch width, FIFO depth, fetch rate -- and nothing imports gowin-profile, so the model cannot drift back toward fit prediction. Cycles are a unit, not a duration: no fmax argument exists and a test asserts the flag stays unknown. Accumulators are bit-exact against spm-gemv-ref across lane counts 1-64 and batches 1-32, including weight_lanes above the row count where lanes wrap onto the next column; the datapath is written out separately from spm-gemv-ref so the differential test compares two implementations rather than one against itself. New measurement: sweeping fetch rate at 8 lanes puts the store-bound to compute-bound crossing between 4 and 16 bytes per cycle, with a datapath floor of 0.125 cycles per weight -- agreeing in direction with step 006's eta, which is the point of having two models. Also corrected a sentence in docs/results.md that contradicted its own table. 122 tests, 0 failed.