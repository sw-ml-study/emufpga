WITHDRAWN, not performed. Step 008 was specified as a resource budget and fit report: per-board LUT4/flip-flop/B-SRAM/DSP utilization, a does-it-fit verdict, and a docs/fit-model.md pinning LUT cost per lane. Mike corrected the scope before any of it was written -- we do not yet want high-fidelity FPGA emulation, we want conceptual testing of something FPGA-like that can be refined later. Device-accurate fit modelling is the opposite of that, and it would also have been unfalsifiable, since gowin-profile records bulk bandwidth and fmax as Unknown for every board. Replaced by step 009, the conceptual fabric model: abstract knobs (lanes, FIFO depth, fetch rate), cycles as a unit rather than a duration, and differential verification against the CPU reference. components/device stays as cited reference data with no consumer.