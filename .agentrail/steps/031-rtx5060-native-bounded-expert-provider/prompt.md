Implement an explicit opt-in bounded native provider for the new GGML mul_mat_id expert-span lease API. Use immutable cache entries, admit-on-second-use, a configurable small byte budget, concurrency-safe hit/miss/loading/eviction state across simultaneous graphs, and never evict a leased entry. Preserve the original mmap fallback when disabled or when an expert exceeds budget. Instrument source bytes, cache hits, wait/load time, evictions, resident and peak bytes. First prove complete Gemma-4 logits are bit-identical to the pass-through and resident controls on CPU. Restore/verify the NVIDIA driver before GPU-placement measurements; then, if available, run the same c1/c2/c4/c8 executable workload under controlled eviction and report correct tasks, tokens/s as usability context, physical/source bytes, RSS/VRAM, and repeated confidence. Update data/docs/live, run gates, commit main, push, and deploy Pages without touching unrelated untracked files.