Implemented the first opt-in bounded native expert provider for the GGML lease hook. The C++ provider uses immutable 64-byte-aligned copies, admits on the second zero-to-active lease transition rather than per-worker callbacks, serializes shared state, applies a configurable byte-bounded LRU, never evicts active leases, and falls back to mmap when disabled, oversized, allocation-limited, or unable to evict safely. Added a focused test proving a leased entry survives competing admission and eight threads share it with balanced releases. Complete three-step Gemma-4 inference at 128 MiB produced 7,432 balanced leases and bit-identical 262,144 logits versus a 1 MiB no-admission control (SHA-256 b4191db5...). The economics were negative for one request: both read 4,181,264,384 logical source bytes; the cache made 610 loads and 549 evictions, showing token-to-token reuse distance exceeds 128 MiB. CUDA access failed inside all llama processes and nvidia-smi became unavailable after one transient success, so no GPU/VRAM or concurrent server campaign was claimed. Updated data/docs/README/live and gates; inherited checklist stayed 9 failures/63 warnings.