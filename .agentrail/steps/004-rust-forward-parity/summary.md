Implemented exact bounded Rust F32/Q6_K decoding, a deterministic single-token Qwen3 embedding plus block-0 forward slice, and a pinned independent llama.cpp logits oracle. Full gate: 252 passed, 0 failed, 14 advisory warnings. CPU oracle is bit-stable; CUDA oracle is blocked until llama.cpp is rebuilt with RTX 5060 Ti compute-12.0 kernel support. Next: capture independent block-0 intermediates, add multi-token RoPE/KV attention, stream all 36 blocks, then compare final logits.