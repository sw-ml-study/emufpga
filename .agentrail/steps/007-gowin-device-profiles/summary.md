Replaced the from-memory device table in docs/plan.md with sourced profiles in components/device/. The open Gowin toolchain is not installed so Apicula was unavailable, and Gowin's DS117-3.2.5E (retrieved) has CID-encoded resource tables that would not extract to text here, so figures come from each board's Sipeed wiki page -- the board vendor's own documentation and the right source for board-level facts anyway. Sourced LUT4/FF/B-SRAM/DSP/PLL for all five boards. Figure is deliberately not Option<u32>: an Option invites unwrap_or(0) and a zero LUT4 count reading as a number is the exact failure the type prevents; every Unknown carries a note saying what was tried. Two gaps recorded rather than filled, both load-bearing for step 008: bulk memory bandwidth and fabric fmax are Unknown for every board, and a test asserts they stay unknown so a future source forces a revisit of the fit model rather than silently changing results. Bandwidth is the figure the whole architecture turns on, since step 006 measured a ~196x compute shortfall against a page-cached read. Also flagged: which 25K board is on hand is unconfirmed (Sipeed sells both a Tang Nano 25K and a Tang Primer 25K); fabric figures are the GW5A-25's from the Primer page and every board-level field is Unknown rather than borrowed. 109 tests, 0 failed.