Scaffolded emufpga: eight components/ placeholder workspaces (no root Cargo.toml), .cargo/config.toml with one shared target/, scripts/{serial.sh,gate.sh,check-locks.sh,check,build} owning every build entry point with a justfile that only delegates, .gitignore/.gitattributes/LICENSE(MIT)/COPYRIGHT, README.md, docs/code_metrics.md (25 LOC/fn, 5 fn/module, 5 modules/crate, 5 crates/component + refactoring lenses), and CLAUDE.md extended below the agentrail markers. Verified the gate against a throwaway workspace instead of assuming: it passes clean and fails correctly on rustfmt and on clippy pedantic, which surfaced two real bugs -- gate.sh passed --workspace to cargo fmt (only accepts --all), and scripts/check skipped sw-checklist when no component had changed. Both fixed. sw-checklist baseline 0 failed / 0 warnings (zero by absence; no Cargo.toml in tree yet).