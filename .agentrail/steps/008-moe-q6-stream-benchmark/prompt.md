Keep Granite Q6_K expert weights packed in the .spm streaming path instead of expanding them to f32. Decode one bounded group immediately before arithmetic, preserve forward-only all-expert dynamic routing and selected-expert correctness against the established GGUF and f32 .spm oracles, and report packed total/useful bytes plus peak resident input memory. Benchmark and separately report artifact emission, cold-read where safely measurable, warm-read, decode, and compute time; derive effective GB/s and tokens/s for batch sizes 1, 4, 8, 16, and 32. Compare blind all-expert sweeps with selected-union expert grouping and explain capacity, bandwidth, and reuse tradeoffs. Preserve the Rust-only no-pickle path, keep large artifacts under /disk1/tmp, run scripts/check --all, commit directly to main, complete Agentrail with a concrete successor, and push main; do not create branches or PRs.