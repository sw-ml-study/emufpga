Finished rung 1. Built TRM's forward pass over streamed weights -- streamed linear batched over positions, resident operators (rms_norm, RoPE, unmasked attention, SwiGLU), and the recursion driving rewind. Then installed torch under uv and compared against the published implementation, which found three bugs no existing test could see: the SwiGLU intermediate width was 2048 instead of 1536 (tests generated shapes from the same wrong formula they were checking), rms_norm normalized the whole state instead of each position (cosine 0.9993 while the stages either side were exact), and every imported matrix was transposed because PyTorch is row-major while .spm stream order is column-major (the round-trip byte test passed throughout, because round-tripping bytes says nothing about how they are interpreted). All four stages now agree with torch at cosine 1.0000000000. Confirmed from source rather than assumed: RoPE theta 10000, split-half rotation, causal=False, contiguous qkv blocks, no learned norm gain. Wrote docs/postmortem-1.md recording that five of eight defects were invisible to a suite passing 150 assertions.