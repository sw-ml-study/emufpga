Focus exclusively on making the existing serial-MoE simulation and measured results interpretable and falsifiable; do not add RTL, synthesis, FPGA vendor tools, or physical-device programming. Build an evidence-first graphical dashboard from current Rust measurements and simulations. Add interactive assumptions for model size, active/total experts, batch, context/KV precision, storage bandwidth, compute rate, and hypothetical clock only when clearly labeled. Graph resident versus serial fast-memory demand, bytes/token or bytes/layer, useful/wasted traffic, latency lower bounds, throughput ceilings, batch/expert-union saturation, and break-even regions. Prominently show good-fit/bad-fit cases and warnings about mistaken assumptions: streaming saves capacity but not automatically time/energy; blind sparse scans can waste most bandwidth; KV can dominate at long context; this small Granite model already fits the GPU; warm page-cache CPU timings are not SSD/GPU/FPGA results; abstract cycles cannot become seconds without clock and memory measurements. Every plotted number must carry measured/derived/projected provenance and link to its formula/source. Prefer visual conclusions over tables, retain accessible numeric details, test calculation/state logic, build pages locally, visually inspect, run scripts/check --all, commit directly to main, complete Agentrail with a concrete successor, push, and deploy pages.