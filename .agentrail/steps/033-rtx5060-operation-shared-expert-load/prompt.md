Redesign the native GGML expert-span integration so each selected expert operation performs at most one cache lookup/load and shares the resulting immutable lease across its worker threads, rather than serializing every worker through the provider mutex. Preserve the barrier lifetime invariant and support simultaneous graphs safely, preferably with operation-scoped shared state or a stable keyed rendezvous. Add stress tests for concurrent operations, no eviction while leased, exact byte identity, and operation/worker telemetry. Rebuild the pinned server and prove deterministic Gemma logits remain bit-identical. Re-run the CPU pilot to determine whether the 40-45% throughput penalty and extreme eviction churn improve. If NVIDIA access is stable, then run alternating repeated c1/c2/c4/c8 same-quant executable campaigns with physical I/O, RSS/PSS, VRAM, and energy; otherwise record the driver blocker and keep GPU claims unchanged. Update data/docs/live, gate, commit main, push, and deploy Pages.