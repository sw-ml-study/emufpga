Measure actual storage demand for the pinned oversized Gemma-4 Q5_K_M resident and reclaimed expert policies without globally dropping caches on the shared host. Implement a narrow file-residency probe/helper using mincore and POSIX_FADV_DONTNEED (or a delegated cgroup-v2 alternative), validate it only targets the pinned model, and capture model SHA/device mapping. On an idle or attributable device, run alternating resident/reclaimed cold and warm trials at concurrency 1/2/4/8 with identical executable Rust prompts and decoding, at least three repetitions where feasible. Record server minor/major faults, per-process IO, device sector deltas or permission-scoped eBPF block IO, RSS/PSS/VRAM, logical expert bytes, passing tasks, and timing. Never use global drop_caches. Distinguish page-cache/DRAM traffic from physical SSD reads and report bytes per generated token/passing task plus SSD-write implications. Speed remains usability, not success. Update checked-in data, cold-cache plan/results, claim scorecard, Gemma analysis, README, and live Results graphics; run full gate without worsening inherited checklist debt; commit main, push, and deploy Pages.