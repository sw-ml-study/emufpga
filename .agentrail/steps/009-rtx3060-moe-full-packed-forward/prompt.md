Extend the validated packed Q6_K .spm MoE schedule from Granite block 0 through all 24 transformer blocks and the final output path. Preserve stable expert-ID order, bounded group decoding, dynamic runtime routing, and forward-only consumption within each physical layer region. Validate per-layer selected experts and representative intermediate outputs where available, then validate final top logits against the established GGUF and llama.cpp-derived oracle. Report total packed artifact size, per-layer total/useful bytes, peak parameter and activation residency, KV-cache contribution for the tested context, rewinds or layer-region transitions, end-to-end timing, and projected RTX 5060 Ti 16 GB capacity. Keep large artifacts under /disk1/tmp, use Rust only with no pickle/Python path, run scripts/check --all, commit directly to main, complete Agentrail with a concrete successor, and push main; do not create branches or PRs.