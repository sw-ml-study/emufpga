Validate a runtime-dynamic MoE schedule without specializing the .spm file to a known route. Implement the clean baseline that stores all Granite block-0 experts in stable expert-ID order, streams every expert forward-only, computes only selected experts after the router decision, and measures total versus useful bytes. Then prototype batching tokens by expert and quantify scan reuse, scheduling-state cost, and the break-even batch size. Compare outputs with the established GGUF and selected-only .spm oracles. Preserve the no-Python/no-pickle path, commit directly to main, run scripts/check --all, complete Agentrail, and push main; do not create branches or PRs.