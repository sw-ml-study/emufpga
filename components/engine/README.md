# components/engine

The SPM streaming engine expressed as a fabric pipeline, its configuration derivation, and differential verification against the saga 1 CPU reference.

Cargo workspace. Filled in by saga 2 (fabric-cycle-model).
See ../../docs/plan.md section 5 for the crate map.
