# components/format

The .spm container: header, ternary/Q4 packing codec, stream directory and op descriptors, reader/writer. A physical execution layout, not a model interchange format.

Cargo workspace. Filled in by saga 1 step 2 (spm-format).
See ../../docs/plan.md section 5 for the crate map.
