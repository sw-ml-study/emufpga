# components/stream

Sequential parameter access. The WeightStream trait has no seek in its surface, so the never-seek-during-an-operation rule is a type-system property. Plus bandwidth, eta, Ps and Rp accounting.

Cargo workspace. Filled in by saga 1 step 3 (spm-stream).
See ../../docs/plan.md section 5 for the crate map.
