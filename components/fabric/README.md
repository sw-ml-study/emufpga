# components/fabric

Behavioral cycle model: blocks (FIFO, decoder, MAC lane, accumulator bank) that each declare their own resource cost, a pipeline elaborator, a cycle scheduler, and an event trace.

Cargo workspace. Filled in by saga 2 (fabric-cycle-model).
See ../../docs/plan.md section 5 for the crate map.
