#!/usr/bin/env bash
set -euo pipefail

root=$(cd "$(dirname "$0")/.." && pwd)
scratch=${SPM_SCRATCH:-/disk1/tmp}
export SPM_GRANITE_FULL_SPM_PATH=${SPM_GRANITE_FULL_SPM_PATH:-$scratch/granite-moe-layer-q6.spm}
exec "$root/scripts/verify-granite-moe" "$@"
